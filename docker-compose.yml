services:
  mkdocs:
    build:
      context: .
      network: host
    ports:
      - "8000:8000"
    volumes:
      - .:/docs
    command: serve --dev-addr 0.0.0.0:8000
    stdin_open: true
    tty: true